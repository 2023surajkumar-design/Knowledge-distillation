# Checkpoints

New experiment checkpoints belong here and are never overwritten. The completed Task 1 teacher remains at the repository root for backward compatibility:

- `resnet34_cifar10_fp32_best.pth`
- `resnet34_cifar10_fp32_final.pth`

Future checkpoints are gitignored; their metadata and SHA-256 must be recorded in `results/`.
