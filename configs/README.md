# Experiment configurations

Each experiment will receive an immutable, fully specified configuration file named with its task, hypothesis, seed, and timestamp. Configurations must record the split fingerprint, teacher checkpoint SHA-256 when applicable, Git SHA, and every optimization/quantization parameter.

No experiment configuration has been created yet: Task 2 is not yet invoked.
