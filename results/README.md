# Results

Store immutable run summaries, history JSON, machine-readable metrics, and per-run manifests here. Model selection is always validation-only. The official test set may be recorded only in a frozen final-report artifact and must never guide a subsequent experiment.
